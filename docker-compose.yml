services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: reelforge_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./backend:/var/www
      - ./backend/storage:/var/www/storage
    environment:
      - APP_ENV=local
    depends_on:
      - mysql
      - redis
    networks:
      - reelforge

  nginx:
    image: nginx:alpine
    container_name: reelforge_nginx
    restart: unless-stopped
    ports:
      - "8000:80"
    volumes:
      - ./backend:/var/www
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app
    networks:
      - reelforge

  mysql:
    image: mysql:8.0
    container_name: reelforge_mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: reelforge
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_PASSWORD: secret
      MYSQL_USER: reelforge
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - reelforge

  redis:
    image: redis:alpine
    container_name: reelforge_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    networks:
      - reelforge

  queue:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: reelforge_queue
    restart: unless-stopped
    working_dir: /var/www
    command: php artisan queue:work redis --sleep=3 --tries=3
    volumes:
      - ./backend:/var/www
    depends_on:
      - mysql
      - redis
    networks:
      - reelforge

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: reelforge_frontend
    restart: unless-stopped
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - VITE_API_URL=http://localhost:8000
    networks:
      - reelforge

networks:
  reelforge:
    driver: bridge

volumes:
  mysql_data:
